<!-- DEBUG: FILTER UI online ✅ -->

<section class="container">
  <div style="border:2px dashed #f60; padding:8px; margin:8px 0;">
    <strong>PLAIN TEST BLOCK</strong> — wenn du das siehst, rendert auch alles **unter** der Debug-Zeile.
  </div>

  <!-- NUR PURES HTML, KEIN CSS/JS/Liquid -->
  <div>
    <p>Test-Buttons (nur HTML):</p>
    <button>Preis</button>
    <button>Farbe</button>
    <button>Marke</button>
    <button>Serie</button>
  </div>
</section>

{% schema %}{ "name": "Collection Filters — Plain Test" }{% endschema %}
