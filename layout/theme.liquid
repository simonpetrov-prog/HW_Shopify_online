<!doctype html>
<html lang="de">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ page_title }} – HW Business</title>
    {{ content_for_header }}
    {{ 'base.css' | asset_url | stylesheet_tag }}
  </head>
  <body>
    <header>
      {% section 'header' %}
    </header>

    <main role="main">
      {{ content_for_layout }}
    </main>

    <footer style="text-align:center; padding:20px; font-size:14px; color:#777;">
      © {{ 'now' | date: "%Y" }} HW Business – powered by Shopify
    </footer>
  </body>
</html>
